# Train the compact TinyCNN backbone from scratch (CPU-friendly, offline).
# Run with: mcv train --config configs/train_tinycnn.yaml
backbone: tinycnn
backbone_pretrained: none
image_size: 96
embedding_dim: 128

loss: supcon
temperature: 0.07

epochs: 16
batch_size: 64
lr: 5.0e-4          # backbone (from scratch); 2e-3 stalls at chance
head_lr: 5.0e-4
weight_decay: 0.02
warmup_steps: 100
freeze_backbone_epochs: 0
hard_negative_mining: true
mining_refresh_every: 2
augment_curriculum: true
curriculum_epochs: 6
views: 2
num_workers: 3
amp: false
seed: 42
val_frac: 0.1
output_dir: ./data/models/tinycnn
